# SA Construction Inc — Logo

Two minimalist redesigns of the original badge logo.

## v2 — Silver blade mark (`v2/`)

A side-view table-saw blade in brushed silver, with a black house set inside it.
The clever bit: **the blade's arbor hole is the house's round window** — one
circle serving both readings. Palette is strictly silver / black / white.

- `v2/sa-construction-mark.svg` — icon only, transparent background
- `v2/sa-construction-logo.svg` — stacked lockup, white background
- `v2/sa-construction-logo-dark.svg` — stacked lockup on black (the house
  reads as a knockout through the blade)
- `v2/sa-construction-horizontal.svg` — horizontal lockup
- `v2/png/` — high-definition PNG exports (up to 2048 px, mark is transparent)

v2 palette: steel gradient `#F5F6F8 → #C3C6CC → #8B8E95`, ink `#0A0A0B`,
white `#FFFFFF`.

## v1 — Line mark

The first pass: a single-line-weight kerf-and-chevron mark. All files are scalable SVG.

## Concept

The original mark (circular saw blade + house + arched text) is reduced to its
essential idea — **precision cutting and homebuilding** — using a single
consistent line weight and generous negative space:

- **The circle** is the blade, drawn as two thin arcs.
- **The horizontal line** is the kerf — the cut passing clean through the blade,
  doubling as the ground line the house sits on.
- **The segmented lower arc** reads as the rim of a diamond saw blade.
- **The chevron + dot** form the house: roofline and door, nothing more.
- Arched text is replaced with letterspaced geometric capitals.

## Files

| File | Use |
|---|---|
| `sa-construction-mark.svg` | Icon only — avatars, favicons, watermarks |
| `sa-construction-logo.svg` | Primary stacked lockup, light backgrounds |
| `sa-construction-logo-dark.svg` | Stacked lockup on black (matches original) |
| `sa-construction-horizontal.svg` | Horizontal lockup — headers, invoices, signage |

## Palette

| Role | Hex |
|---|---|
| Ink | `#1A1A1A` |
| Cream (dark version) | `#EFE9DE` |
| Background (dark version) | `#0C0C0C` |
